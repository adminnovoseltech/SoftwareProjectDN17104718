<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.novoseltech.handymano">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.novoseltech.handymano.HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/Theme.MaterialComponents.Light.NoActionBar" />
        <activity android:name="com.novoseltech.handymano.BusinessRegistrationActivity" />
        <activity android:name="com.novoseltech.handymano.UserRegistrationActivity" />
        <activity android:name="com.novoseltech.handymano.RegistrationChoiceActivity" />
        <activity android:name="com.novoseltech.handymano.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>